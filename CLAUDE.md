# stock_analyzer — 한국 주식 촉매 레이더

> 상위 폴더(`~/Downloads/CLAUDE.md`)는 editorial-copy-ai(패션 카피 SaaS) 지침이다.
> **이 폴더와 무관하니 무시할 것.** 이 파일이 이 프로젝트의 지침이다.

## 한 문장

매일 아침 뉴스를 읽고 → AI가 촉매를 점수화해 "아직 안 오른" 종목을 추천하고 →
그 추천의 실제 성적을 자동 채점해 다시 화면에 붙여주는 시스템.
구조 전체는 [SYSTEM.md](SYSTEM.md) 한 장에 정리돼 있다.

## 소유자

- 개발자가 아닌 바이브코더. **코드 결정과 구현은 Claude가 주도하되, 왜 이렇게 하는지 한국어로 짧게 설명**할 것
- 통계 용어를 쓸 땐 한 줄로 뜻을 붙일 것 (예: "t=2 이상이면 우연이 아닐 가능성이 높다는 뜻")

## 절대 규칙 — 검증

이 프로젝트가 실패하는 방식은 단 하나, **없는 엣지를 있다고 착각하는 것**이다.
따라서 성과 주장을 하기 전에 아래를 전부 통과해야 한다.

1. **대조군 없는 성과 주장 금지.** "지수 대비 알파"는 증거가 아니다.
   반드시 *같은 날·같은 시장·같은 워치리스트*에서 무작위로 뽑은 종목과 비교한다.
   → `venv/bin/python -m tools.control_test`
2. **날짜 클러스터 t값으로 판단.** 같은 날 픽 40개는 독립 표본 40개가 아니라 사실상 1개다.
   픽 수(n=1000)로 유의성을 주장하면 거의 항상 틀린다. 유효 표본은 **거래일 수**.
3. **벤치마크 창은 실제 보유 구간과 일치**해야 한다. 손절로 D+1에 나왔으면
   지수도 D+1까지만 잰다. (창 불일치는 하락장에서 알파를 통째로 만들어낸다)
4. **거래비용을 항상 반영.** 기본 왕복 0.4%(세금+수수료+슬리피지). 0%는 금지.
5. **손절 체결은 갭 반영.** 시초가가 손절선 아래면 시초가 체결(-10% 고정 아님).
6. **표본 레짐을 명시.** 한 레짐이 총수익을 지배하면 총수익 랭킹은 실력을 못 가린다.
7. **유니버스는 시점 정보로만 구성.** "오늘 시총 상위"로 과거를 백테스트하면 미래를 안다.
   (동일가중 +94.9% → 시점기준으로 고치니 **+2.3%**. 성과의 대부분이 편향이었다)
   → 거래대금(종가×거래량)은 캔들에 있으니 매 시점 다시 뽑는다.
8. **전략 성과는 리밸런싱 시작일 offset 전체의 중앙값·범위로 보고.** 단일 숫자 금지.
   (같은 전략이 시작일만 바꿔 지수대비 −52%p ~ +87%p. 중앙값은 +5.6%p였다)
9. **전략과 벤치마크는 같은 날짜로.** 전략이 건너뛴 날은 벤치마크도 빼야 한다.
10. **다중검정 보정.** 가설 N개를 던지면 임계값도 N배 엄격하게(본페로니).

## 검증 도구 (모든 성과 주장의 관문)

```bash
venv/bin/python -m tools.control_test      # 픽 vs 같은날·같은시장 대조군
venv/bin/python -m tools.hypothesis_lab    # 횡단면 팩터 (본페로니·홀드아웃·단조성)
venv/bin/python -m tools.strategy_lab      # 전략 그리드 + 레짐 분해
venv/bin/python -m tools.timing_lab        # 지수 추세 타이밍 + 워크포워드
venv/bin/python -m tools.final_test2       # 시점기준 유니버스 (생존편향 제거)
venv/bin/python -m tools.fetch_flows       # 수급(외인·기관) 수집
venv/bin/python -m tools.flow_test         # 수급 가설 검정
venv/bin/python -m tools.flow_strategy     # 수급+모멘텀 전략
```
`tools/honest_backtest.py` — offset 스윕·배당·현금이자를 강제하는 공용 프로토콜.
**새 백테스트는 여기 `sweep()`/`report()`를 쓸 것.** 단일 숫자를 반환하지 않는다.

## 감사 이력

- **2026-08-20 대조군 감사** — 촉매 픽이 같은 날·같은 시장 무작위 종목 대비
  **−2.7%p 열위(t=−6.2, 9개 설정 전부 음수)**. 즉 AI 촉매점수는 현재 엣지가 아니라 역엣지.
  SYSTEM.md ③의 "확정된 운용 규칙"은 위 1~5를 통과하지 않은 수치이므로 **폐기 대상**.
  상세: [docs/audit_20260820.md](docs/audit_20260820.md)
- **2026-08-20 가설 재설계 1차** — 309거래일 패널로 가설 12개 검증.
  `mom_20`(20일 모멘텀)만 생존(탐색 t=4.67/검증 t=4.73, 생존편향·레짐 무관).
  단 **절대수익은 지수보다 못하다**(+112% vs KOSPI +178%, MDD −42%).
  이 기간 수익을 지배한 건 종목선택이 아니라 **사이즈**였다.
  상세: [docs/findings_20260820.md](docs/findings_20260820.md)
- **2026-08-28 전면 재검정 (가설 17개)** — 가격축 15개 + 수급축.
  결론: **시총가중 KOSPI 단순보유를 이기는 설정이 없다** (0/24, 0/16).
  단 **신호 하나는 확정** — "모멘텀 상위 + 기관 순매수 아닌 종목"이 같은 유니버스
  동일가중을 검증구간 **+35%p, 모든 offset에서** 이긴다 (lag5 생존, 이중정렬 통과).
  격차의 정체는 신호가 아니라 **가중방식**(초대형주 집중).
  상세: [docs/verdict_20260828.md](docs/verdict_20260828.md) ·
  [docs/flow_findings.md](docs/flow_findings.md) ·
  [docs/rejected_hypotheses.md](docs/rejected_hypotheses.md)

## 운영

- Python은 **`venv/bin/python`** (시스템 python3엔 deps 없음)
- UI dev: `cd ui && /opt/homebrew/opt/node@20/bin/npm run dev` (node@20 필수)
- 원격 main에 cron이 계속 push → 작업 전 `git fetch && git reset --hard origin/main`.
  **코드만 커밋하고 생성물(`reports/*.json`, `state/*.jsonl`)은 cron 소유로 discard.**
- 토스 키는 로컬 `.env` / `ui/.env.local`. IP 화이트리스트 방식 → 빈 응답이면 IP 차단 의심
- cron엔 토스 키 없음 → 네이버 폴백(약 40거래일)으로 동작
- **DART는 병렬 수집에 IP를 즉시 차단한다.** 2026-08-28에 워커 4개로 16초에 ~2,600요청을
  날렸다가 `RemoteDisconnected`로 막혔다. `tools/fetch_dart.py`는 이제 단일 워커 +
  요청 간 대기가 기본이고 차단을 감지하면 멈춘다. **차단 중 저장된 "0건"은 휴장일과
  구별되지 않으므로 반드시 버리고 다시 받을 것.** 재시도는 30분 이상 뒤에.
- 네이버도 마찬가지 — 워커 4개·요청 간 0.12~0.15초를 넘기지 말 것

## 코드 관례

- 새 분석 도구는 `tools/`에 `python -m tools.xxx`로 실행되게 추가
- 파라미터를 바꿨으면 **왜 바꿨는지·A/B 근거를 주석으로** 남긴다 (기존 `SPIKE_DAYS` 주석 스타일)
- 프론트(`ui/`)는 소유자가 직접 만지므로 과한 추상화 금지

## 하지 말 것

- 개별 종목 매수/매도 조언 (시스템·통계 이야기만)
- 검증 안 된 규칙을 SYSTEM.md "확정된 규칙"에 올리는 것
- 픽 수를 늘려 유의성을 만들어내는 것 (거래일 수가 늘어야 의미가 있다)
