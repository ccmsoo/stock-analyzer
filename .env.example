# API 키 설정 — 이 파일을 `.env`로 복사하고 값을 채우세요
# .env는 .gitignore로 제외되므로 절대 commit되지 않습니다

# OpenAI (기본)
OPENAI_API_KEY=sk-proj-...

# Anthropic (사용시 AI_PROVIDER=anthropic 도 설정)
# ANTHROPIC_API_KEY=sk-ant-...
# AI_PROVIDER=anthropic
